apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-devops-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: node-devops-app
  template:
    metadata:
      labels:
        app: node-devops-app
    spec:
      containers:
      - name: node-app
        image: rohit151/node-devops-app:latest
        ports:
        - containerPort: 3000
